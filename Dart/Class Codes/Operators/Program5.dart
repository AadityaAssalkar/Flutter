// Logical Operators
// this operators only operates on boolean value/s

void main(){
  int x = 10;
  int y = 8;

  print(x && y); // x and y are not boolean values therefore error for all print statemet
  print(x || y);
  print(!x);
  print(!y);
}